{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline permissions for the app's own windows (main + settings), local asset content only.",
  "windows": ["main", "settings"],
  "permissions": [
    "core:default",
    "notification:default",
    "notification:allow-notify",
    "updater:default"
  ]
}
